# 所有值都转成bool值  有一个false就是false  全是True就是True
print(all(['hello', 'good', 'yes', 'no']))

print(all(['dsa',0]))

nus = [1,2,3,549]
print(dir(nus))
def te (a,b):
    '''

    :param a:
    :param b:
    :return:
    '''
    return 0
help(te)  # 不可加括号  只是函数名